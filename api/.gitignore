# Dependencies
node_modules/

# Build
dist/
*.tsbuildinfo

# Environment (keep .env.example committed)
.env
.env.*
!.env.example

# Test / coverage
coverage/

# Deploy config — rendered from *.template files by setup.sh; never commit the live versions
deploy/systemd/book-camera-api.service
deploy/nginx/book-camera.conf

# Local CI → droplet private keys (see DEPLOY.md)
book-camera-deploy-key*

# Logs & OS
*.log
.DS_Store
